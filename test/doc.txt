

 在test下启动 node server(静态服务器,因为ajax请求是需要服务器环境 )
 然后在根目录下启动 node server1(目标服务器)
 
 在这个例子中， server 在3000 端口启动的相当于代理服务器，它将我们所有的 ajax 请求进行了转发，然后将结果返回
 
                server1 在8889 端口启动的相当于目标服务器，由于我们是通过 3000 端口做转发，所以我们并不知道真实的数据是来自于 8889 端口，也就是隐藏了真实服务器
				
				这种将请求统一由代理服务器转发给目标服务器的技术，就是 反向代理
